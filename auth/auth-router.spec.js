const request = require('supertest');
const router = require('./auth-router');

describe('jokes', function() {
    it('tells a funny not-so-funny joke', function () {
        expect(true).toBe(true);
    })
})